# i18n (UK / EN) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Ukrainian/English language switching to Deberc — toggle in GameHeader, all UI text translated, preference persisted in localStorage.

**Architecture:** Bundled TypeScript locale objects (`src/i18n/locales/uk.ts` + `en.ts`) initialized synchronously via i18next. React state `lang` in App.tsx controls the active language; `i18n.changeLanguage()` triggers re-renders. No HTTP requests, fully PWA/offline-compatible.

**Tech Stack:** `i18next` + `react-i18next`, TypeScript structural typing for locale completeness, `useTranslation()` hook in each component.

---

### Task 1: Install i18next + react-i18next

**Files:**
- Modify: `package.json` (via bun add)

- [ ] **Step 1: Install packages**

```bash
bun add i18next react-i18next
```

- [ ] **Step 2: Verify install**

```bash
bun run type-check
```
Expected: same errors as before (no new errors from install).

- [ ] **Step 3: Commit**

```bash
git add package.json bun.lockb
git commit -m "chore: add i18next + react-i18next"
```

---

### Task 2: Create translation files

**Files:**
- Create: `src/i18n/locales/uk.ts`
- Create: `src/i18n/locales/en.ts`
- Create: `src/i18n/index.ts`

- [ ] **Step 1: Create `src/i18n/locales/uk.ts`**

```ts
export const uk = {
  app: {
    title: 'Деберц',
  },
  header: {
    gameInfo: 'Гра #{{id}} · до {{score}} очок',
    soundOff: 'Вимкнути звук',
    soundOn: 'Увімкнути звук',
    newGame: 'Нова гра',
    langToggle: 'EN',
    confirmNewGame: 'Розпочати нову гру?',
    confirmDesc: 'Поточний прогрес буде втрачено.',
  },
  setup: {
    playersLabel: 'Гравців',
    targetLabel: 'До перемоги',
    target510: '510 — швидка',
    target1020: '1020 — класика',
    players2: '2 гравці',
    players3: '3 гравці',
    players4: '4 гравці',
    playersSection: 'Гравці',
    dealerHint: '(👑 = дилер)',
    rulesLabel: 'Правила',
    startGame: '🎴 Почати гру',
    penaltyBLabel: 'Штраф за 2-гу "Б"',
    penaltyHVLabel: 'Штраф за "ХВ"',
  },
  round: {
    roundLabel: 'Раунд',
    placeholder: '0, Б, ХВ, ВІС',
    placeholderNoVis: '0, Б, ХВ',
    scoreFor: 'Рахунок для {{name}}',
    setToken: 'Встановити {{token}} для {{name}}',
    fillAll: '⏳ Заповніть всі поля',
    addRound: '✅ Додати раунд',
  },
  history: {
    title: 'Історія раундів',
    empty: 'Поки що немає завершених раундів',
    toggle: 'Показати або сховати історію раундів',
    total: 'Всього: {{n}}',
    undoBtn: 'Undo',
    undoAriaLabel: 'Скасувати останній раунд',
    round: 'Раунд {{n}}',
    dealer: 'Д: {{name}}',
    preview: 'перегляд',
    edit: '✏️ Редагувати',
    save: 'Зберегти',
    cancelEdit: 'Відмінити',
    invalidHint: 'Вкажіть числа або Б / ХВ / ВІС',
    confirmUndo: 'Скасувати останній раунд?',
    confirmUndoDesc: 'Раунд буде видалено, рахунки повернуться до попереднього стану.',
    undoConfirmLabel: 'Скасувати раунд',
  },
  score: {
    title: 'Рахунок',
  },
  winner: {
    title: 'ПЕРЕМОЖЕЦЬ',
    scoreRounds: '{{score}} очок · {{rounds}} раундів',
    summary: 'Підсумок',
    newGame: '🎮 Нова гра',
    continue: '▶ Продовжити',
  },
  recover: {
    finishedGame: 'Завершена гра #{{id}}',
    unfinishedGame: 'Незавершена гра #{{id}}',
    roundInfo: 'Раунд {{n}} · до {{score}} очок',
    viewResult: '🏆 Переглянути результат',
    continue: '▶ Продовжити гру',
    newGame: 'Нова гра',
  },
  common: {
    cancel: 'Скасувати',
  },
  stats: {
    title: 'Детальна статистика',
    rounds: '{{n}} раундів',
    avg: 'Середній',
    roundsStat: 'Раундів',
    best: 'Кращий',
    worst: 'Гірший',
  },
  gameHistory: {
    title: 'Історія ігор',
    wins: {
      one: 'перемога',
      few: 'перемоги',
      many: 'перемог',
    },
    games: {
      one: 'гра',
      few: 'гри',
      many: 'ігор',
    },
  },
  error: {
    oneB: 'Лише один гравець може отримати Б за раунд.',
    oneVis: 'Лише один гравець може грати ВіС за раунд.',
    invalidScores: 'Заповніть всі поля валідними значеннями (число, Б, ХВ або ВІС).',
    invalidScoresNoVis: 'Заповніть всі поля валідними значеннями (число, Б, ХВ).',
  },
};
```

- [ ] **Step 2: Create `src/i18n/locales/en.ts`**

```ts
import type { uk } from './uk';

export const en: typeof uk = {
  app: {
    title: 'Deberc',
  },
  header: {
    gameInfo: 'Game #{{id}} · to {{score}} pts',
    soundOff: 'Mute sound',
    soundOn: 'Enable sound',
    newGame: 'New Game',
    langToggle: 'УК',
    confirmNewGame: 'Start a new game?',
    confirmDesc: 'Current progress will be lost.',
  },
  setup: {
    playersLabel: 'Players',
    targetLabel: 'Play to',
    target510: '510 — quick',
    target1020: '1020 — classic',
    players2: '2 players',
    players3: '3 players',
    players4: '4 players',
    playersSection: 'Players',
    dealerHint: '(👑 = dealer)',
    rulesLabel: 'Rules',
    startGame: '🎴 Start Game',
    penaltyBLabel: 'Penalty for 2nd "Б"',
    penaltyHVLabel: 'Penalty for "ХВ"',
  },
  round: {
    roundLabel: 'Round',
    placeholder: '0, Б, ХВ, ВІС',
    placeholderNoVis: '0, Б, ХВ',
    scoreFor: 'Score for {{name}}',
    setToken: 'Set {{token}} for {{name}}',
    fillAll: '⏳ Fill in all fields',
    addRound: '✅ Add round',
  },
  history: {
    title: 'Round History',
    empty: 'No completed rounds yet',
    toggle: 'Show or hide round history',
    total: 'Total: {{n}}',
    undoBtn: 'Undo',
    undoAriaLabel: 'Undo last round',
    round: 'Round {{n}}',
    dealer: 'D: {{name}}',
    preview: 'preview',
    edit: '✏️ Edit',
    save: 'Save',
    cancelEdit: 'Cancel',
    invalidHint: 'Enter numbers or Б / ХВ / ВІС',
    confirmUndo: 'Undo last round?',
    confirmUndoDesc: 'The round result will be removed.',
    undoConfirmLabel: 'Undo round',
  },
  score: {
    title: 'Score',
  },
  winner: {
    title: 'WINNER',
    scoreRounds: '{{score}} pts · {{rounds}} rounds',
    summary: 'Summary',
    newGame: '🎮 New Game',
    continue: '▶ Continue',
  },
  recover: {
    finishedGame: 'Finished game #{{id}}',
    unfinishedGame: 'Unfinished game #{{id}}',
    roundInfo: 'Round {{n}} · to {{score}} pts',
    viewResult: '🏆 View Result',
    continue: '▶ Continue Game',
    newGame: 'New Game',
  },
  common: {
    cancel: 'Cancel',
  },
  stats: {
    title: 'Detailed Statistics',
    rounds: '{{n}} rounds',
    avg: 'Average',
    roundsStat: 'Rounds',
    best: 'Best',
    worst: 'Worst',
  },
  gameHistory: {
    title: 'Game History',
    wins: {
      one: 'win',
      few: 'wins',
      many: 'wins',
    },
    games: {
      one: 'game',
      few: 'games',
      many: 'games',
    },
  },
  error: {
    oneB: 'Only one player can take Б per round.',
    oneVis: 'Only one player can play ВіС per round.',
    invalidScores: 'Fill in all fields with valid values (number, Б, ХВ, or ВІС).',
    invalidScoresNoVis: 'Fill in all fields with valid values (number, Б, or ХВ).',
  },
};
```

- [ ] **Step 3: Create `src/i18n/index.ts`**

```ts
import i18next from 'i18next';
import { initReactI18next } from 'react-i18next';
import { uk } from './locales/uk';
import { en } from './locales/en';

declare module 'i18next' {
  interface CustomTypeOptions {
    resources: { translation: typeof uk };
  }
}

i18next.use(initReactI18next).init({
  lng: (localStorage.getItem('lang') as 'uk' | 'en') ?? 'uk',
  fallbackLng: 'uk',
  resources: {
    uk: { translation: uk },
    en: { translation: en },
  },
  interpolation: { escapeValue: false },
});
```

- [ ] **Step 4: Run type-check to confirm files parse**

```bash
bun run type-check
```
Expected: no errors in the new files.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/
git commit -m "feat(i18n): add translation files and i18next initialization"
```

---

### Task 3: Wire i18n into main.tsx and App.tsx

**Files:**
- Modify: `src/main.tsx` — add `import './i18n'` side-effect
- Modify: `src/App.tsx` — add `lang` state + `handleLangChange` + translate error messages + pass props to GameHeader

- [ ] **Step 1: Add i18n import in `src/main.tsx`**

Add `import './i18n';` as the first import (before React):

```tsx
import './i18n';
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './styles/index.css';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
```

- [ ] **Step 2: Add `lang` state and `handleLangChange` to `src/App.tsx`**

After the existing `SOUND_KEY` constant (line ~20), add:
```ts
const LANG_KEY = 'lang';
```

After the `soundEnabled` state line (~line 50), add:
```ts
const { i18n, t } = useTranslation();
const [lang, setLang] = useState<'uk' | 'en'>(() =>
  (localStorage.getItem(LANG_KEY) as 'uk' | 'en') ?? 'uk'
);
const handleLangChange = () => {
  const next: 'uk' | 'en' = lang === 'uk' ? 'en' : 'uk';
  setLang(next);
  localStorage.setItem(LANG_KEY, next);
  i18n.changeLanguage(next);
};
```

Also add the import at the top of App.tsx:
```ts
import { useTranslation } from 'react-i18next';
```

- [ ] **Step 3: Replace error messages in `src/App.tsx`**

Find the three `setError(...)` calls (lines ~168, 172, 176) and replace:

```ts
// line ~168
setError(t('error.oneB'));
// line ~172
setError(t('error.oneVis'));
// line ~176
setError(gameRules.allowVis ? t('error.invalidScores') : t('error.invalidScoresNoVis'));
```

- [ ] **Step 4: Pass `lang` and `onLangChange` to GameHeader in `src/App.tsx`**

Find the `<GameHeader` JSX block (line ~408) and add two props:
```tsx
<GameHeader
  gameId={game.id}
  targetScore={targetScore}
  dealerName={game.players.find((p) => p.id === game.dealerId)?.name || ''}
  onNewGame={resetGame}
  hasRounds={game.rounds.length > 0}
  soundEnabled={soundEnabled}
  onSoundToggle={() => setSoundEnabled((prev) => !prev)}
  lang={lang}
  onLangChange={handleLangChange}
/>
```

- [ ] **Step 5: Type-check**

```bash
bun run type-check
```
Expected: errors about `lang`/`onLangChange` not existing in GameHeaderProps — these will be fixed in Task 4.

- [ ] **Step 6: Commit (after Task 4 compiles clean)**

Skip this commit — commit together with Task 4.

---

### Task 4: Language toggle in GameHeader

**Files:**
- Modify: `src/components/GameHeader.tsx` — add `lang`/`onLangChange` props + toggle button + translate hardcoded strings

- [ ] **Step 1: Replace entire `src/components/GameHeader.tsx`**

```tsx
import React, { useState } from 'react';
import { useTranslation } from 'react-i18next';
import { ConfirmSheet } from './ConfirmSheet';

interface GameHeaderProps {
  gameId: number;
  targetScore: number;
  dealerName?: string;
  onNewGame?: () => void;
  hasRounds?: boolean;
  soundEnabled?: boolean;
  onSoundToggle?: () => void;
  lang?: 'uk' | 'en';
  onLangChange?: () => void;
}

export const GameHeader: React.FC<GameHeaderProps> = ({
  gameId,
  targetScore,
  dealerName,
  onNewGame,
  hasRounds = false,
  soundEnabled = true,
  onSoundToggle,
  lang = 'uk',
  onLangChange,
}) => {
  const { t } = useTranslation();
  const [confirming, setConfirming] = useState(false);

  const handleNewGame = () => {
    if (hasRounds) {
      setConfirming(true);
    } else {
      onNewGame?.();
    }
  };

  return (
    <>
      <div className="relative rounded-2xl overflow-hidden bg-card-bg border border-white/8 p-4">
        {/* Watermark suits */}
        <div className="absolute inset-0 flex items-center justify-center text-7xl text-white/4 font-display pointer-events-none select-none tracking-widest" aria-hidden="true">
          ♠ ♥ ♦ ♣
        </div>

        <div className="relative z-10 flex items-center justify-between">
          <div>
            <h1 className="font-display text-2xl gold-gradient-text">{t('app.title')}</h1>
            <p className="text-muted text-sm">
              {t('header.gameInfo', { id: gameId, score: targetScore })}
            </p>
          </div>

          <div className="flex items-center gap-2">
            {dealerName && (
              <div className="flex items-center gap-2 bg-white/5 border border-white/10 rounded-full px-3 py-1.5">
                <span className="text-sm">👑</span>
                <span className="text-sm text-white/80 font-sans">{dealerName}</span>
              </div>
            )}

            {onLangChange && (
              <button
                type="button"
                onClick={onLangChange}
                aria-label={lang === 'uk' ? 'Switch to English' : 'Перейти на Українську'}
                className="w-9 h-9 rounded-xl bg-card-bg border border-white/10 text-xs font-bold text-white/70
                  hover:border-white/30 hover:text-white transition-all duration-150 active:scale-[0.97]
                  flex items-center justify-center"
              >
                {t('header.langToggle')}
              </button>
            )}

            {onSoundToggle && (
              <button
                type="button"
                onClick={onSoundToggle}
                aria-label={soundEnabled ? t('header.soundOff') : t('header.soundOn')}
                className="w-9 h-9 rounded-xl bg-card-bg border border-white/10 text-base
                  hover:border-white/30 transition-all duration-150 active:scale-[0.97]
                  flex items-center justify-center"
              >
                {soundEnabled ? '🔊' : '🔇'}
              </button>
            )}

            {onNewGame && (
              <button
                type="button"
                onClick={handleNewGame}
                className="px-4 py-2 rounded-xl bg-card-bg border border-white/10 text-muted text-sm font-semibold
                  hover:border-white/30 hover:text-white transition-all duration-150 active:scale-[0.97]
                  focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-to/60"
              >
                {t('header.newGame')}
              </button>
            )}
          </div>
        </div>
      </div>

      {confirming && (
        <ConfirmSheet
          title={t('header.confirmNewGame')}
          description={t('header.confirmDesc')}
          confirmLabel={t('header.newGame')}
          onConfirm={() => { setConfirming(false); onNewGame?.(); }}
          onCancel={() => setConfirming(false)}
        />
      )}
    </>
  );
};

export default GameHeader;
```

- [ ] **Step 2: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no type errors, 107 tests pass.

- [ ] **Step 3: Commit Tasks 3 + 4 together**

```bash
git add src/main.tsx src/App.tsx src/components/GameHeader.tsx
git commit -m "feat(i18n): wire i18n into App, add lang toggle to GameHeader"
```

---

### Task 5: Translate SetupScreen

**Files:**
- Modify: `src/components/SetupScreen.tsx`

- [ ] **Step 1: Add `useTranslation` and replace all hardcoded strings**

Replace the entire file content:

```tsx
import React, { useState } from 'react';
import { useTranslation } from 'react-i18next';
import { GameRulesConfig } from '../types';
import { ChipGroup } from './ChipGroup';
import { GoldDivider } from './GoldDivider';
import { Button } from './Button';
import { PlayerRow } from './PlayerRow';
import { PenaltySheet } from './PenaltySheet';

interface SetupScreenProps {
  playerCount: number;
  onPlayerCountChange: (count: number) => void;
  targetScore: number;
  onTargetScoreChange: (score: number) => void;
  names: string[];
  onNamesChange: (names: string[]) => void;
  dealerIndex: number;
  onDealerIndexChange: (index: number) => void;
  gameRules: GameRulesConfig;
  onRulesChange: (rules: GameRulesConfig) => void;
  playerNames?: string[];
  onStart: () => void;
}

type PenaltyKey = 'secondBPenalty' | 'hvPenalty';

export const SetupScreen: React.FC<SetupScreenProps> = ({
  playerCount,
  onPlayerCountChange,
  targetScore,
  onTargetScoreChange,
  names,
  onNamesChange,
  dealerIndex,
  onDealerIndexChange,
  gameRules,
  onRulesChange,
  playerNames = [],
  onStart,
}) => {
  const { t } = useTranslation();
  const [penaltySheet, setPenaltySheet] = useState<PenaltyKey | null>(null);

  const PLAYER_COUNT_OPTIONS = [
    { label: t('setup.players2'), value: 2 as const },
    { label: t('setup.players3'), value: 3 as const },
    { label: t('setup.players4'), value: 4 as const },
  ];

  const handlePlayerCountChange = (count: number) => {
    onPlayerCountChange(count);
    const next = [...names];
    while (next.length < count) next.push('');
    onNamesChange(next.slice(0, count));
    if (dealerIndex >= count) onDealerIndexChange(0);
  };

  const handleNameChange = (idx: number, value: string) => {
    const next = [...names];
    next[idx] = value;
    onNamesChange(next);
  };

  const targetOptions = [
    { label: t('setup.target510'), value: 510 as const },
    { label: t('setup.target1020'), value: 1020 as const },
    ...(gameRules.customTargetScore
      ? gameRules.targetScoreOptions
          .filter((s) => s !== 510 && s !== 1020)
          .map((s) => ({ label: String(s), value: s as number }))
      : []),
  ];

  const canStart = names.every((n) => n.trim().length > 0);

  return (
    <div className="w-full max-w-md mx-auto flex flex-col gap-6 py-6 px-4">
      {/* Header */}
      <div className="text-center relative">
        <div className="absolute inset-0 flex items-center justify-center text-8xl text-white/5 font-display pointer-events-none select-none" aria-hidden="true">
          ♥♦♣
        </div>
        <h1 className="font-display text-4xl gold-gradient-text relative z-10">{t('app.title')} ♠</h1>
      </div>

      {/* Кількість гравців */}
      <section>
        <h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">{t('setup.playersLabel')}</h2>
        <ChipGroup
          options={PLAYER_COUNT_OPTIONS}
          value={playerCount}
          onChange={handlePlayerCountChange}
        />
      </section>

      {/* До перемоги */}
      <section>
        <h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">{t('setup.targetLabel')}</h2>
        <ChipGroup
          options={targetOptions}
          value={targetScore}
          onChange={onTargetScoreChange}
        />
      </section>

      <GoldDivider />

      {/* Імена гравців */}
      <section>
        <h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">
          {t('setup.playersSection')} <span className="normal-case opacity-60">{t('setup.dealerHint')}</span>
        </h2>
        <div className="flex flex-col gap-3">
          {names.map((name, idx) => (
            <PlayerRow
              key={idx}
              index={idx}
              name={name}
              isDealer={dealerIndex === idx}
              onNameChange={(v) => handleNameChange(idx, v)}
              onSetDealer={() => onDealerIndexChange(idx)}
              suggestions={playerNames}
            />
          ))}
        </div>
      </section>

      <GoldDivider />

      {/* Правила */}
      <section>
        <h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">{t('setup.rulesLabel')}</h2>
        <div className="flex flex-wrap gap-2">
          {/* ВІС toggle */}
          <button
            type="button"
            onClick={() => onRulesChange({ ...gameRules, allowVis: !gameRules.allowVis })}
            aria-pressed={gameRules.allowVis}
            className={`px-4 py-2 rounded-full text-sm font-semibold border transition-all duration-150 active:scale-[0.97]
              focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-to/60
              ${gameRules.allowVis
                ? 'bg-token-vis/20 border-token-vis text-token-vis'
                : 'bg-card-bg border-white/10 text-muted hover:border-white/30'
              }`}
          >
            ВІС {gameRules.allowVis ? '✓' : '✗'}
          </button>

          {/* 2-га Б penalty */}
          <button
            type="button"
            onClick={() => setPenaltySheet('secondBPenalty')}
            className="px-4 py-2 rounded-full text-sm font-semibold border bg-card-bg border-white/10 text-score-neg hover:border-score-neg/50 transition-all duration-150 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-to/60"
          >
            {t('setup.penaltyBLabel')}: {gameRules.secondBPenalty}
          </button>

          {/* ХВ penalty */}
          <button
            type="button"
            onClick={() => setPenaltySheet('hvPenalty')}
            className="px-4 py-2 rounded-full text-sm font-semibold border bg-card-bg border-white/10 text-score-neg hover:border-score-neg/50 transition-all duration-150 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-to/60"
          >
            {t('setup.penaltyHVLabel')}: {gameRules.hvPenalty}
          </button>
        </div>
      </section>

      {/* Start button */}
      <Button
        fullWidth
        disabled={!canStart}
        onClick={onStart}
        className="py-4 text-lg mt-2"
      >
        {t('setup.startGame')}
      </Button>

      {/* Penalty sheet */}
      {penaltySheet && (
        <PenaltySheet
          label={penaltySheet === 'secondBPenalty' ? t('setup.penaltyBLabel') : t('setup.penaltyHVLabel')}
          value={gameRules[penaltySheet]}
          onChange={(v) => onRulesChange({ ...gameRules, [penaltySheet]: v })}
          onClose={() => setPenaltySheet(null)}
        />
      )}
    </div>
  );
};

export default SetupScreen;
```

- [ ] **Step 2: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no errors, 107 tests pass.

- [ ] **Step 3: Commit**

```bash
git add src/components/SetupScreen.tsx
git commit -m "feat(i18n): translate SetupScreen"
```

---

### Task 6: Translate RoundForm

**Files:**
- Modify: `src/components/RoundForm.tsx`

- [ ] **Step 1: Add `useTranslation` and replace hardcoded strings in `RoundForm.tsx`**

Add import at the top:
```ts
import { useTranslation } from 'react-i18next';
```

Inside the `RoundForm` component (after props destructuring), add:
```ts
const { t } = useTranslation();
```

Replace:
```ts
// OLD:
const placeholder = allowVis ? '0, Б, ХВ, ВІС' : '0, Б, ХВ';
// NEW:
const placeholder = allowVis ? t('round.placeholder') : t('round.placeholderNoVis');
```

In the `<input>` element, replace `aria-label`:
```tsx
// OLD:
aria-label={`Рахунок для ${p.name}`}
// NEW:
aria-label={t('round.scoreFor', { name: p.name })}
```

In the chip `<button>`, replace `aria-label`:
```tsx
// OLD:
aria-label={`Встановити ${token} для ${p.name}`}
// NEW:
aria-label={t('round.setToken', { token, name: p.name })}
```

In the header span, replace `'Раунд'`:
```tsx
// OLD:
<span className="text-white font-sans font-semibold">Раунд</span>
// NEW:
<span className="text-white font-sans font-semibold">{t('round.roundLabel')}</span>
```

In the add button, replace text:
```tsx
// OLD:
{isAddDisabled ? '⏳ Заповніть всі поля' : '✅ Додати раунд'}
// NEW:
{isAddDisabled ? t('round.fillAll') : t('round.addRound')}
```

- [ ] **Step 2: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no errors, 107 tests pass.

- [ ] **Step 3: Commit**

```bash
git add src/components/RoundForm.tsx
git commit -m "feat(i18n): translate RoundForm"
```

---

### Task 7: Translate RoundHistory

**Files:**
- Modify: `src/components/RoundHistory.tsx`

- [ ] **Step 1: Add `useTranslation` import and hook**

Add at the top:
```ts
import { useTranslation } from 'react-i18next';
```

Inside component, after props destructuring:
```ts
const { t } = useTranslation();
```

- [ ] **Step 2: Replace all hardcoded Ukrainian strings**

Empty state (line ~70):
```tsx
// OLD:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">Історія раундів</h2>
<p className="text-muted text-sm py-4">Поки що немає завершених раундів</p>
// NEW:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">{t('history.title')}</h2>
<p className="text-muted text-sm py-4">{t('history.empty')}</p>
```

Header toggle area:
```tsx
// OLD:
aria-label="Показати або сховати історію раундів"
// NEW:
aria-label={t('history.toggle')}
```

```tsx
// OLD:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest">Історія раундів</h2>
// NEW:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest">{t('history.title')}</h2>
```

Undo button:
```tsx
// OLD:
aria-label="Скасувати останній раунд"
// ...
↩ Undo
// NEW:
aria-label={t('history.undoAriaLabel')}
// ...
↩ {t('history.undoBtn')}
```

Total badge:
```tsx
// OLD:
<span className="text-muted text-xs bg-white/5 px-2 py-0.5 rounded-full">Всього: {rounds.length}</span>
// NEW:
<span className="text-muted text-xs bg-white/5 px-2 py-0.5 rounded-full">{t('history.total', { n: rounds.length })}</span>
```

Round number label:
```tsx
// OLD:
<span ...>Раунд {round.number}</span>
// NEW:
<span ...>{t('history.round', { n: round.number })}</span>
```

Dealer badge:
```tsx
// OLD:
<span ...>Д: {dealerName}</span>
// NEW:
<span ...>{t('history.dealer', { name: dealerName })}</span>
```

Preview badge:
```tsx
// OLD:
<span ...>перегляд</span>
// NEW:
<span ...>{t('history.preview')}</span>
```

Edit button:
```tsx
// OLD:
✏️ Редагувати
// NEW:
{t('history.edit')}
```

Invalid hint:
```tsx
// OLD:
<p ...>Вкажіть числа або Б / ХВ / ВІС</p>
// NEW:
<p ...>{t('history.invalidHint')}</p>
```

Edit action buttons:
```tsx
// OLD:
Відмінити
// NEW:
{t('history.cancelEdit')}

// OLD:
Зберегти
// NEW:
{t('history.save')}
```

Undo ConfirmSheet props:
```tsx
// OLD:
title="Скасувати останній раунд?"
description="Раунд буде видалено, рахунки повернуться до попереднього стану."
confirmLabel="Скасувати раунд"
// NEW:
title={t('history.confirmUndo')}
description={t('history.confirmUndoDesc')}
confirmLabel={t('history.undoConfirmLabel')}
```

- [ ] **Step 3: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no errors, 107 tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/components/RoundHistory.tsx
git commit -m "feat(i18n): translate RoundHistory"
```

---

### Task 8: Translate WinnerScreen + RecoverScreen

**Files:**
- Modify: `src/components/WinnerScreen.tsx`
- Modify: `src/components/RecoverScreen.tsx`

- [ ] **Step 1: Translate `WinnerScreen.tsx`**

Add import:
```ts
import { useTranslation } from 'react-i18next';
```

Inside component, after props destructuring:
```ts
const { t } = useTranslation();
```

Replace strings:
```tsx
// OLD:
<h1 className="font-display text-3xl gold-gradient-text tracking-wide">ПЕРЕМОЖЕЦЬ</h1>
// NEW:
<h1 className="font-display text-3xl gold-gradient-text tracking-wide">{t('winner.title')}</h1>
```

```tsx
// OLD:
<p className="text-muted text-sm">
  {totals[String(winner.id)] ?? 0} очок · {roundCount} раундів
</p>
// NEW:
<p className="text-muted text-sm">
  {t('winner.scoreRounds', { score: totals[String(winner.id)] ?? 0, rounds: roundCount })}
</p>
```

```tsx
// OLD:
<span className="text-muted text-xs uppercase tracking-widest font-semibold">Підсумок</span>
// NEW:
<span className="text-muted text-xs uppercase tracking-widest font-semibold">{t('winner.summary')}</span>
```

```tsx
// OLD:
<Button fullWidth onClick={onNewGame} className="py-4 text-base">
  🎮 Нова гра
</Button>
<Button fullWidth variant="secondary" onClick={onContinue} className="py-3 text-base">
  ▶ Продовжити
</Button>
// NEW:
<Button fullWidth onClick={onNewGame} className="py-4 text-base">
  {t('winner.newGame')}
</Button>
<Button fullWidth variant="secondary" onClick={onContinue} className="py-3 text-base">
  {t('winner.continue')}
</Button>
```

- [ ] **Step 2: Translate `RecoverScreen.tsx`**

Add import:
```ts
import { useTranslation } from 'react-i18next';
```

Inside component, after props destructuring:
```ts
const { t } = useTranslation();
```

Replace strings:
```tsx
// OLD:
<h1 className="font-display text-4xl gold-gradient-text">Деберц <span aria-hidden="true">♠</span></h1>
// NEW:
<h1 className="font-display text-4xl gold-gradient-text">{t('app.title')} <span aria-hidden="true">♠</span></h1>
```

```tsx
// OLD:
<h2 className="font-display text-xl gold-gradient-text">
  {isFinished ? `Завершена гра #${game.id}` : `Незавершена гра #${game.id}`}
</h2>
// NEW:
<h2 className="font-display text-xl gold-gradient-text">
  {isFinished ? t('recover.finishedGame', { id: game.id }) : t('recover.unfinishedGame', { id: game.id })}
</h2>
```

```tsx
// OLD:
<p className="text-muted text-sm mt-0.5">
  Раунд {roundCount} · до <span className="text-white font-semibold">{targetScore}</span> очок
</p>
// NEW:
<p className="text-muted text-sm mt-0.5">
  {t('recover.roundInfo', { n: roundCount, score: targetScore })}
</p>
```

```tsx
// OLD:
<Button fullWidth onClick={onRecover} className="py-4 text-lg">
  {isFinished ? '🏆 Переглянути результат' : '▶ Продовжити гру'}
</Button>
// NEW:
<Button fullWidth onClick={onRecover} className="py-4 text-lg">
  {isFinished ? t('recover.viewResult') : t('recover.continue')}
</Button>
```

```tsx
// OLD:
Нова гра (inside the secondary button)
// NEW:
{t('recover.newGame')}
```

- [ ] **Step 3: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no errors, 107 tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/components/WinnerScreen.tsx src/components/RecoverScreen.tsx
git commit -m "feat(i18n): translate WinnerScreen and RecoverScreen"
```

---

### Task 9: Translate ScoreBoard + ConfirmSheet + PlayerStatistics + GameHistory

**Files:**
- Modify: `src/components/ScoreBoard.tsx`
- Modify: `src/components/ConfirmSheet.tsx`
- Modify: `src/components/PlayerStatistics.tsx`
- Modify: `src/components/GameHistory.tsx`

- [ ] **Step 1: Translate `ScoreBoard.tsx`**

Add import:
```ts
import { useTranslation } from 'react-i18next';
```

Inside `ScoreBoard` component (NOT inside `PlayerCard`), add:
```ts
const { t } = useTranslation();
```

Replace:
```tsx
// OLD:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">Рахунок</h2>
// NEW:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest mb-3">{t('score.title')}</h2>
```

- [ ] **Step 2: Translate `ConfirmSheet.tsx`**

`ConfirmSheet` currently uses an implicit-return arrow function. Convert it to explicit return and add the hook:

```tsx
import React from 'react';
import { useTranslation } from 'react-i18next';

// ... (keep ConfirmSheetProps interface unchanged)

export const ConfirmSheet: React.FC<ConfirmSheetProps> = ({
  title,
  description,
  confirmLabel,
  onConfirm,
  onCancel,
}) => {
  const { t } = useTranslation();
  return (
    // ... (same JSX as before, only change "Скасувати" → {t('common.cancel')})
  );
};
```

Replace in the cancel button:
```tsx
// OLD:
Скасувати
// NEW:
{t('common.cancel')}
```

- [ ] **Step 3: Translate `PlayerStatistics.tsx`**

Add import:
```ts
import { useTranslation } from 'react-i18next';
```

Inside `PlayerStatistics` component, add:
```ts
const { t } = useTranslation();
```

Replace the header h2:
```tsx
// OLD:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest flex-1">Детальна статистика</h2>
// NEW:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest flex-1">{t('stats.title')}</h2>
```

Replace the rounds count badge:
```tsx
// OLD:
{game.rounds.length} раундів
// NEW:
{t('stats.rounds', { n: game.rounds.length })}
```

Replace stat labels inside `allStats.map(...)`:
```tsx
// OLD:
<StatCell label="Середній" ... />
<StatCell label="Раундів" ... />
<StatCell label="Кращий" ... />
<StatCell label="Гірший" ... />
// NEW:
<StatCell label={t('stats.avg')} ... />
<StatCell label={t('stats.roundsStat')} ... />
<StatCell label={t('stats.best')} ... />
<StatCell label={t('stats.worst')} ... />
```

- [ ] **Step 4: Translate `GameHistory.tsx`**

Add import:
```ts
import { useTranslation } from 'react-i18next';
```

Inside `GameHistory` component, add:
```ts
const { t } = useTranslation();
```

Replace `victoryLabel` function to use translations:
```ts
// OLD:
const victoryLabel = (n: number): string => {
  if (n === 1) return 'перемога';
  if (n >= 2 && n <= 4) return 'перемоги';
  return 'перемог';
};
// NEW:
const victoryLabel = (n: number): string => {
  if (n === 1) return t('gameHistory.wins.one');
  if (n >= 2 && n <= 4) return t('gameHistory.wins.few');
  return t('gameHistory.wins.many');
};
```

Replace `gamesLabel`:
```ts
// OLD:
const gamesLabel = totalGames === 1 ? 'гра' : totalGames < 5 ? 'гри' : 'ігор';
// NEW:
const gamesLabel = totalGames === 1
  ? t('gameHistory.games.one')
  : totalGames < 5
    ? t('gameHistory.games.few')
    : t('gameHistory.games.many');
```

Replace the title h2:
```tsx
// OLD:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest flex-1">Історія ігор</h2>
// NEW:
<h2 className="text-muted text-xs font-semibold uppercase tracking-widest flex-1">{t('gameHistory.title')}</h2>
```

- [ ] **Step 5: Type-check + test**

```bash
bun run type-check && bun run test
```
Expected: no errors, 107 tests pass.

- [ ] **Step 6: Commit**

```bash
git add src/components/ScoreBoard.tsx src/components/ConfirmSheet.tsx src/components/PlayerStatistics.tsx src/components/GameHistory.tsx
git commit -m "feat(i18n): translate ScoreBoard, ConfirmSheet, PlayerStatistics, GameHistory"
```

---

### Task 10: Final verification and cleanup

**Files:**
- No new file changes expected — verification only.

- [ ] **Step 1: Run full lint + auto-fix**

```bash
bun run lint:fix
```
Expected: no unfixable errors.

- [ ] **Step 2: Run type-check**

```bash
bun run type-check
```
Expected: 0 errors.

- [ ] **Step 3: Run all tests**

```bash
bun run test
```
Expected: 107 tests pass.

- [ ] **Step 4: Grep for remaining hardcoded Ukrainian UI strings**

```bash
grep -rn "Гра\|Нова гра\|Раунд\|Скасувати\|Зберегти\|Гравець\|очок\|раундів\|Переможець\|Підсумок" src/components/ src/App.tsx | grep -v "node_modules\|\.test\." | grep -v "//\|\/\*"
```
Expected: only strings inside `t(...)` calls, comments, or game-term variables. Fix any missed hardcoded strings.

- [ ] **Step 5: Commit if any fixups were made, then final commit**

```bash
git add -p
git commit -m "feat(i18n): complete UK/EN language support"
```
